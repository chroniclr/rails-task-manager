<ul>
  <% @tasks.each do |task| %>
  <li>
    <%= link_to task.title, show_path(task) %> | <%= task.details %> | <%= task.completed %>
  </li>
  <% end %>

</ul>
<p><%= link_to "Add a new task", new_path %></p>
